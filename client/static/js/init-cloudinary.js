/*
For some reason putting the below code in the html doesn't run the code..

<script>
var cl = new cloudinary.Cloudinary({cloud_name: "votetoplay", secure: true});
</script>
*/
var cl = new cloudinary.Cloudinary({cloud_name: "votetoplay", secure: true});
